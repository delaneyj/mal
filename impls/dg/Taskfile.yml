# https://taskfile.dev

version: '3'

interval: 100ms

vars:
  GREETING: Hello, World!

tasks:
  default:
    sources:
      - "**/*.go"
    cmds:
      - go mod tidy
      - cd ../.. && make "test^dg^step1"
    silent: true
